import { LegalPage } from "@/components/legal-page";

export default function TermsPage() {
  return (
    <LegalPage title="Terms">
      <p>
        A base purchase unlocks the current campaign run only. Starting a new run
        or replay may require a separate purchase.
      </p>
    </LegalPage>
  );
}
