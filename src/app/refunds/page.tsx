import { LegalPage } from "@/components/legal-page";

export default function RefundsPage() {
  return (
    <LegalPage title="Refunds">
      <p>
        Refunds and disputes revoke only the entitlement created by the originating
        order while preserving the run history for support review.
      </p>
    </LegalPage>
  );
}
